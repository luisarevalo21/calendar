package hw2;

import java.awt.FlowLayout;
import java.io.File;
import java.io.IOException;
import java.text.ParseException;
import java.util.Scanner;

import javax.swing.Icon;
import javax.swing.JFrame;
import javax.swing.JLabel;

/**
 * 
 * @author aldonut
 * tester class for calendar
 *
 */
public class MyCalendarTester {
	
	//Scanner in = new Scanner(System.in);
    

	
	public static void main(String[] args) throws ParseException, IOException
	{
		//Scanner in = new Scanner(System.in);

		//Controller cal = new Controller(in);
		
		/*
		String option = "";
		do{
		option = in.nextLine();
		
		
		switch(option)
		{
		case "l": 
		      File eventsTxt = new File("events.txt");

			if(!eventsTxt.exists())
				System.out.println("There is no file yet since"
						+ " this is the first run");
			else
					cal.load();
		break;
		case "v": 
			System.out.println("[d]ay view or [m]view ? ");
		    String choiceView = in.nextLine();
		    cal.view(choiceView);
		
		break;
		case "c": cal.addEvent(in);
		break;
		case "g": cal.goTo(in);
		break;
		case "e": cal.allEvents();
		break;
		case "d": cal.delete(in);
		break;
		case "q": cal.quit();
		break;
		}
		}
		while(!option.equals("q"));
		*/
		

	}
}
